# ============================================================================
#  Fast adresse for tunnelen.
#
#  Leses av install.sh, tunnel-setup.sh, status.sh og reset.sh.
#  Ingen hemmeligheter her — selve nokkelen ligger i ~/.cloudflared/ pa
#  maskinen som kjorer tunnelen, og skal aldri inn i repoet.
#
#  Er tunnelen ikke satt opp enna (deploy/tunnel-setup.sh), faller
#  install.sh tilbake til en hurtigtunnel med tilfeldig trycloudflare-adresse.
# ============================================================================

TUNNEL_NAME=musicstreamer
TUNNEL_HOST=multiroom.jwk.no
